### AL ###
#launch.json folder (user specific)
.vscode/
#Cache folder (temporary)
.alcache/
#Symbols folder (large files and can be retrieved if required)
.alpackages/
#Snapshots folder (for debugging, temporary)
.snapshots/
#Testing Output folder(test or output files can be stored - no need to track)
.output/
#Extension App-file (final app file - should be built from source code)
*.app
#Rapid Application Development File (temporary)
rad.json
#Translation Base-file (automatically generated)
*.g.xlf
#License-file (environment specific - no need to track)
*.flf
#Test results file (file for test results - no need to track unless speficially desired)
TestResults.xml